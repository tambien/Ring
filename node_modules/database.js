/*
 * DATABASE
 */

var keys = require("./keys");

(function() {
	
	//database functions

	//the public API
    module.exports.getPost = function(req, res, next) {
        res.send("a new lookup");
		next();
    }
    
    module.exports.addPost = function(post) {
    	   
    }
    
}());